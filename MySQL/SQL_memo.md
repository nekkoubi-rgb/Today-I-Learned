#SQL基本文法

- `SELECT` `DISTINCT` **列名のリスト**
    - `DISTINCT` 内容が完全に一致している行(重複)を一行にまとめる

- `FROM`
    **表名のリスト**

- `WHERE`
    行の**選択条件**
- `GROUP BY` **グループ化に用いる列名**のリスト

- `HAVING` グループ化の**条件**

- `ORDER BY` 整列に用いる列名、昇順降順指定

